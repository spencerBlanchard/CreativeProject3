import winnerImg from '../Media/greenbutton.png';

const Win = () => {
  return (
    <div>
      <img src={winnerImg} alt="If this gif loaded, you'd be dead." />
    </div>

  );

};

export default Win;